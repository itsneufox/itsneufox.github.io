source 'https://rubygems.org'

gem "jekyll", "~> 4.3.3"          # installed by `gem jekyll`
gem "just-the-docs", "0.9.0"      # pinned to the current release
# gem "just-the-docs"             # always download the latest release

# Additional plugins used in your configuration
gem "jekyll-seo-tag"              # for SEO metadata
gem "jekyll-feed"                 # for generating RSS feeds
gem "jekyll-sitemap"              # for generating sitemaps

# Optional: Add webrick if you are using Ruby 3.x and above
# gem "webrick"                    # required when using Ruby >= 3 and Jekyll <= 4.2.2
